# Adding a city

Create a manifest with identity, locale, timezone, bounds, data URL, attribution and capability flags. Implement the municipality’s adapter under `scripts/city/adapters/`. Validate stable IDs, WGS84 coordinates, units and 12-bit phenology masks before building the pack.
