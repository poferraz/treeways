const line = () => ({ type: 'Feature', properties: {}, geometry: { type: 'LineString', coordinates: [] } });
export function addRouteLayers(map) { map.addSource('route', { type: 'geojson', data: line() }); map.addSource('route-stops', { type: 'geojson', data: { type: 'FeatureCollection', features: [] } }); map.addLayer({ id: 'route-line', type: 'line', source: 'route', paint: { 'line-color': '#2474a6', 'line-width': 5, 'line-opacity': 0.9 } }); map.addLayer({ id: 'route-stops', type: 'circle', source: 'route-stops', paint: { 'circle-color': '#2474a6', 'circle-radius': 9, 'circle-stroke-color': '#fff', 'circle-stroke-width': 2 } }); }
export function setRouteData(map, geometry) { map.getSource('route')?.setData(geometry ? { type: 'Feature', properties: {}, geometry } : line()); }
