const FILTERS = [['all', 'All curated'], ['edible', 'Edible'], ['blossoms', 'Blossoms'], ['blooming', 'Blooming now'], ['harvesting', 'Harvesting now']];
export function createFilters({ onChange }) { const fieldset = document.createElement('fieldset'); fieldset.className = 'filters'; const legend = document.createElement('legend'); legend.textContent = 'Tree filters'; fieldset.append(legend); for (const [value, label] of FILTERS) { const button = document.createElement('button'); button.type = 'button'; button.textContent = label; button.dataset.filter = value; if (value === 'all') button.setAttribute('aria-pressed', 'true'); button.addEventListener('click', () => { fieldset.querySelectorAll('button').forEach(item => item.setAttribute('aria-pressed', String(item === button))); onChange(value); }); fieldset.append(button); } return fieldset; }
